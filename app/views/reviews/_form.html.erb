<%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :rating, options_for_select([1, 2, 3, 4, 5])%>
  <%= form.input :content %>
  <%= form.button :submit, class: "btn-primary" %>
<% end %>
